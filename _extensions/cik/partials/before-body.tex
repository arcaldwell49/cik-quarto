$-- Implements the frontmatter, title page, and abstract.
$--
$-- %%%% TODO %%%%%
$-- Customize is needed, otherwise remove this partials to use Quarto default one
$-- %%%%%%%%%%%%%%%%
\captionsetup[table]{labelformat=empty}
\captionsetup[figure]{labelformat=empty}
\raggedbottom

%%%% Article title to be placed here
\title{$title$}
\subtitle{
{$subtitle$}
}

%\author{
%$for(by-author)$
% $it.name.literal$\textsuperscript{$it.number$}$sep$,
%$endfor$
%}

$for(by-author)$
  \author[$for(by-author.affiliations)$$it.number$$sep$,$endfor$$if(by-author.email)$,*$endif$]
  {$by-author.name.literal$}
$endfor$

$for(by-affiliation)$
\affil[$it.number$]{$it.name$}
$endfor$

$for(by-author)$
$if(by-author.email)$
  \corrauthor[*]{$by-author.email$}
$endif$
$endfor$

\address{ \\ }
%%%% Subject entries to be placed here %%%%
\subject{
$for(sci-subject)$
$sci-subject$$sep$,
$endfor$
}

%%%% Keyword entries to be placed here %%%%
\keywords{
$for(keywords)$
$keywords$$sep$,
$endfor$
}

%%%% Insert corresponding author and its email address}
%\corres{
%  $corresp_author_name$\\
%{\footnotesize $corresp_author_email$}
%}
\inserttype{
{\Large $article_type$}
}

\insertcite{
  {$doi$}
}

$if(notetf)$
  \risenote{
  { $printnote$}
  }
$endif$

\editor{
  $journal-editor$
}

%%%% Abstract text to be placed here %%%%%%%%%%%%
\begin{abstract}
$abstract$
\end{abstract}
%%%%%%%%%%%%%%%%%%%%%%%%%%%

%% Some pieces required from the pandoc template
\providecommand{\tightlist}{%
  \setlength{\itemsep}{0pt}\setlength{\parskip}{0pt}}
\providecommand{\EndFirstPage}{%
}

\definecolor{jobcolor}{cmyk}{0,0,0,.95}
\definecolor{joblightcolor}{cmyk}{0,0,0,.95}
\definecolor{abstractcolor}{RGB}{207, 250, 209}
\definecolor{copyrightcolor}{RGB}{207, 250, 209}


\maketitle

\newpage

$if(graphics)$
%\usepackage{graphicx}
\makeatletter
\def\maxwidth{\ifdim\Gin@nat@width>\linewidth\linewidth\else\Gin@nat@width\fi}
\def\maxheight{\ifdim\Gin@nat@height>\textheight\textheight\else\Gin@nat@height\fi}
\makeatother
% Scale images if necessary, so that they will not overflow the page
% margins by default, and it is still possible to overwrite the defaults
% using explicit options in \includegraphics[width, height, ...]{}
\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
% Set default figure placement to htbp
\makeatletter
\def\fps@figure{htbp}
\makeatother
$endif$

%\setkeys{Gin}{width=\maxwidth,height=\maxheight,keepaspectratio}
% Set default figure placement to htbp
\makeatletter
\def\fps@figure{htbp}
\makeatother